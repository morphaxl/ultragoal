Add machine-readable output to the todo CLI (todo.mjs): `node todo.mjs list --json` must print exactly one JSON array of objects {"id": number, "text": string, "done": boolean}, ordered by id ascending, and nothing else on stdout — an empty list prints []. Everything that exists today must keep working byte-for-byte: plain `list` keeps its current human-readable format, `add` and `done` keep their output and exit codes, and unknown commands still exit with status 2.