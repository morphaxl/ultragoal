slugify.mjs only handles the happy path (lowercase + spaces to dashes) and violates its contract on real-world titles — punctuation leaks through, accents are lost, repeated separators pile up, and there is no length handling. SPEC.md defines the full required behavior in 7 numbered rules with examples. Make slugify conform to SPEC.md for ALL inputs, including the edge cases. Keep the module's interface exactly as is: a default-exported function taking one string. Do not change SPEC.md.