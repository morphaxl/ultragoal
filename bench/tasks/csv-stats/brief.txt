csvstats.mjs computes wrong statistics: means are off, and empty or missing CSV cells corrupt min/max/mean (run `node demo.mjs` to see it). USAGE.md documents the intended contract with a worked example. Fix csvstats.mjs so the documented contract holds for all inputs — empty and missing cells are absent values excluded from statistics, columns with no present values report null stats, and means are correct. Keep the exported API exactly as is: named exports parseCSV and columnStats with the same signatures. Do not change USAGE.md.